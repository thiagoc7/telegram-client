{{#if isReposting}}

  <div class="post-block clearfix confirm-repost">
    <img class="avatar" src="images/icon-retweet.png"/>

    <div class="content">
      <div class="question">Repost this to your followers?</div>
      <div class="footer">
        <a class="blue-link" {{action 'confirmRepost'}}>Yes</a>
        <span class="middot">&middot;</span>
        <a class="blue-link" {{action 'toggleRepost'}}>No</a>
      </div>
    </div>
  </div>

{{else}}

  <div class="post-block clearfix">

    {{#if post.repostedFrom}}
      <div class="repost-header clearfix">
        <img class="icon" src="images/icon-retweet.png"/>
        <div class="author">
          Reposted by
          {{#link-to 'user' post.author classNames='dark-gray-link'}}
            {{post.author.name}}
          {{/link-to}}
        </div>
      </div>
    {{/if}}

    {{#link-to 'user' authorDisplay classNames='avatar link'}}
      <img {{bind-attr src=authorDisplay.avatar}}>
    {{/link-to}}

    <div class="content">
      <div class="author">
        {{#link-to 'user' authorDisplay classNames='link'}}
          {{authorDisplay.name}}
        {{/link-to}}
      </div>
      <div class="text">{{post.body}} {{reposted}}</div>
      <div class="footer">
        <span>{{ago post.createdAt}}</span>
        <span class="middot">&middot;</span>

        {{#if post.repostedFrom}}
          <span>Reposted</span>
        {{else}}

          {{#if owned}}
            <a class="blue-link" {{action 'showModal'}}>Edit</a>
          {{else}}
            <a class="blue-link" {{action 'toggleRepost'}}>Repost</a>
          {{/if}}

        {{/if}}

        {{#if owned}}
          <span class="middot">&middot;</span>
          <a class="blue-link" {{action 'deletePost'}}>Delete</a>
        {{/if}}

      </div>
    </div>
  </div>

{{/if}}
